[global]
# Use newer pip features and avoid legacy setuptools issues
use-feature = fast-deps
prefer-binary = true

[install]
# Force use of wheels when possible to avoid compilation issues
only-binary = :all:
# Allow fallback to source for packages without wheels
trusted-host = pypi.org
               pypi.python.org
               files.pythonhosted.org